# Ce fichier est maintenu pour la compatibilité
# Pour les nouveaux environnements, utilisez plutôt :
# pip install -e .[dev]  # Pour le développement
# pip install -e .[test]  # Pour les tests
# pip install -e .  # Pour l'installation de base

# Core
fastapi>=0.100.0  # Version compatible avec Pydantic 2.x
uvicorn[standard]>=0.23.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
pymysql>=1.1.0  # Connexion MySQL pour SQLAlchemy
# Ancienne dépendance PostgreSQL (conservée pour référence)
# psycopg2-binary>=2.9.5

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-magic>=0.4.27

# API & HTTP
httpx>=0.25.0
pydantic>=2.5.0,<3.0.0  # Version majeure 2.x
aiohttp>=3.8.5
python-socketio>=5.9.0

# Audio Processing
mutagen>=1.46.0
pydub>=0.25.1

# Utilities
aiofiles>=23.1.0
python-slugify>=7.0.0
python-dateutil>=2.8.2
tqdm>=4.65.0
pydantic-settings>=2.0.0
