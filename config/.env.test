# Configuration de test
# Ce fichier est utilisé pour les tests unitaires et d'intégration

# Configuration de base
DEBUG=True
TESTING=True

# Configuration de la base de données
DATABASE_URI=sqlite+aiosqlite:///:memory:

# Configuration JWT
SECRET_KEY=test-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60  # 1 heure pour les tests

# Désactive la vérification SSL pour les tests
VERIFY_SSL=False

# Désactive les notifications par email dans les tests
SEND_EMAILS=False

# Configuration VoidAuth pour les tests
VOIDAUTH_SERVER_URL=http://voidauth:8080
VOIDAUTH_REALM=audionexus
VOIDAUTH_CLIENT_ID=audionexus-backend
VOIDAUTH_CLIENT_SECRET=test-client-secret
VOIDAUTH_ADMIN_USER=admin
VOIDAUTH_ADMIN_PASSWORD=test
